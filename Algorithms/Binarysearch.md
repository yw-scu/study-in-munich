# 二分查找
## 框架
```
def binarySearch(nums,terget):
    left=0,right=...
    while ...:
        mid=left+int((right-left)/2)#防止溢出
        if nums[mid]==target:
            ...
        elif nums[mid]<target:
            left=...
        elif nums[mid]>target:
            right=...
    return ...
```
## 实例
### 基本的二叉搜索
```

```
